@model IEnumerable<Vitality.Models.BloodGroup>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BloodGroup1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BloodGroup1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BloodGroupId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BloodGroupId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BloodGroupId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
