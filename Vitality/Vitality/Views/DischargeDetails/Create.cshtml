@{
    var errorMessage = TempData["ErrorMessage"] as string;
    if (!string.IsNullOrEmpty(errorMessage))
    {
        <script>
            alert('@errorMessage');
        </script>
    }
}
@{
    Layout = "~/Views/Shared/_adminsLayout.cshtml";
}
@model Vitality.Models.DischargeDetail

@{
    ViewData["Title"] = "Discharge Details";
}

<div class="page-wrapper">
    <div class="content container-fluid">

        <div class="page-header">
            <div class="row">
                <div class="col-sm-12">
                    <h3 class="page-title">Discharge Details Information</h3>
                    @*<ul class="breadcrumb">
                    <li class="breadcrumb-item"><a href="index.html">Dashboard</a></li>
                    <li class="breadcrumb-item"><a href="javascript:(0)">Settings</a></li>
                    <li class="breadcrumb-item active">General Settings</li>
                    </ul>*@
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-12">

                <div class="card">
                    <div class="card-header">
                        <h4 class="card-title">Discharge Details</h4>
                    </div>
                    <div class="card-body">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label class="control-label">Patient's Card ID</label>
                <select asp-for="DischargePatientsCardId" class ="form-control" asp-items="ViewBag.DischargePatientsCardId"></select>
            </div>
            <div class="form-group">
                <label class="control-label">Discharge From</label>
                <input asp-for="DischargeFrom" required class="form-control" />
                <span asp-validation-for="DischargeFrom" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Add Detail" class="btn btn-primary" />
            </div>
        </form>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>