@model IEnumerable<Vitality.Models.HospitalBilling>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BillFor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BillFor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HospitalBillings">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HospitalBillings">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HospitalBillings">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
